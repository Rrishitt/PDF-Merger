# PDF-Merger
A simple PDF-merging script that asks the user how many PDFs they want to combine, collects filenames, and uses PyPDF2.PdfWriter to merge them into a single output file named merged-pdf.pdf.
